# CData Arc MCP Server Configuration

# Required: Base URL for your CData Arc instance
CDATA_BASE_URL=http://localhost:8181/api.rsc

# Required: Authentication token for Arc API access
CDATA_AUTH_TOKEN=your-auth-token-here

# Optional: Transport mode for MCP server (stdio or http)
MCP_TRANSPORT_MODE=stdio

# Optional: Port for HTTP transport (only used when MCP_TRANSPORT_MODE=http)
MCP_HTTP_PORT=3000